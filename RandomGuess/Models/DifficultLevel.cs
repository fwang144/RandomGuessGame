public enum DifficultyLevel
{
    /// <summary>
    /// Easy difficulty level, requires guesses between the range 0 and 10.
    /// </summary>
    Easy,

    /// <summary>
    /// Medium difficulty level, requires guesses between the range 0 and 100.
    /// </summary>
    Medium,

    /// <summary>
    /// Hard difficulty level, requires guesses between the range 0 and 1000.
    /// </summary>
    Hard
}